The global variable is declared outside of main() function, which gives it a global scope, meaning it can be used and modified from any part of the program. 

The local variable is declared inside of main() function, which gives it a local scope, meaning it can only be used and modified inside main(). 
